RewriteEngine On

# Remove .html from URL internally load .php
RewriteRule ^([a-zA-Z0-9_-]+)\.html$ $1.php [L]

# Optional: Redirect .php URL to .html
RewriteCond %{THE_REQUEST} \s/([^.]+)\.php[\s?] [NC]
RewriteRule ^ /%1.html [R=301,L]